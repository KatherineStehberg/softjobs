//const validateParameterUser = (req, res, next) => {
    //const { user } = req.body;
    //if (!user.email || !user.password || !user.nombre || !user.apellido || !user.lenguaje || !user.rol) {
       // return res.status(400).json({ error: "El email, password, nombre, apellido, lenguaje y rol deben estar presentes" });
    //}
    //next();
//};

//export { validateParameterUser };
